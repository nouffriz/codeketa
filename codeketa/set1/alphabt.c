#include <stdio.h>

int main(void) {
					char ch;
					scanf("%s",&ch);
						if((ch >=97 && ch <=122)||(ch >=65 && ch <=90))
						{
							printf("alphabet",ch);
						}
						else
						printf("no",ch);
	return 0;
}
